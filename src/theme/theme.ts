"use client";
import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#002349",
    },
    secondary: {
      main: "#ea8225",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
